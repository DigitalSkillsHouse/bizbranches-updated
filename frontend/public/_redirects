# SPA fallback: serve index.html for all routes so listing detail URLs (e.g. /digital-skills-house) work.
# Netlify: place this in publish dir (e.g. out/). Copy from public to out after build if needed.
/*    /index.html   200
