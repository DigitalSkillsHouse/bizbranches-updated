# ===========================================
# BizBranches - Environment Variables
# NEVER commit .env or real secrets.
#
# For cPanel deployment, set these as GitHub Secrets
# and in the .env files on your server.
# ===========================================

# ─── PHP BACKEND (backend-php/.env on cPanel) ────────────
# See backend-php/.env.example for full backend config

# ─── FRONTEND (frontend/.env.local) ──────────────────────
# Backend API URL - used for server-side fetches and rewrites
NEXT_PUBLIC_BACKEND_URL=http://localhost:3002
BACKEND_URL=http://localhost:3002

# Cloudinary (client-side image URLs)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name

# Site URL
NEXT_PUBLIC_SITE_URL=https://bizbranches.pk

# ─── PRODUCTION VALUES (set in GitHub Secrets) ───────────
# NEXT_PUBLIC_BACKEND_URL=https://api.bizbranches.pk
# BACKEND_URL=https://api.bizbranches.pk
# SITE_URL=https://bizbranches.pk

# ─── GITHUB SECRETS REQUIRED FOR CI/CD ───────────────────
# FTP_SERVER=ftp.bizbranches.pk
# FTP_USERNAME=your_cpanel_username
# FTP_PASSWORD=your_cpanel_password
# FTP_FRONTEND_DIR=/home/cpaneluser/bizbranches.pk/
# FTP_BACKEND_DIR=/home/cpaneluser/api.bizbranches.pk/
# BACKEND_URL=https://api.bizbranches.pk
# SITE_URL=https://bizbranches.pk
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# SSH_HOST=bizbranches.pk      (only for migrations)
# SSH_USERNAME=cpanel_user      (only for migrations)
# SSH_PASSWORD=cpanel_pass      (only for migrations)
